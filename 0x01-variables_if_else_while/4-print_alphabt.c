#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x = 'a';

	while (x <= 'z')
	{
		if ((x != 'e') && (x != 'q')0
			{
				putchar(x);
				x++;
			}
	}
		putchar('n');
		return (0);
}
